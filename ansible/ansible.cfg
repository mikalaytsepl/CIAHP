[defaults]
inventory=inventory.yml
remote_user=ansible
private_key_file=~/.ssh/ansible

# ansible-galaxy collection install kubernetes.core -p ./collections
# use ansible-galaxy collection install kubernetes.core -p ./collections --force
# if it's still installed elsewhere
# for now change it to the absolute path of your machine, will be changed in prod
collections_path = /home/miko/CIAHP/ansible/collections
# after golden image template will be created this would be no longer needed
host_key_checking = False